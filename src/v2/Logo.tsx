/* SiteRápido logo — official horizontal lockup (mark + wordmark). */
export function Logo() {
  return (
    <img
      className="v2-logo-img"
      src="/logo-siterapido.png"
      alt="siterapido.me"
      width={184}
      height={26}
    />
  );
}
